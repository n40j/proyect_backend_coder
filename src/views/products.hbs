<!DOCTYPE html>
<html>
<head>
  <title>Lista de Productos</title>
</head>
<body>
  <h1>Lista de Productos</h1>
  <ul>
    {{#each products}}
      <li>{{this.name}} - Precio: {{this.price}}</li>
      <button onclick="agregarAlCarrito('{{this._id}}')">Agregar al Carrito</button>
    {{/each}}
  </ul>

  <div>
    {{#if hasPrevPage}}
      <a href="/products?page={{prevPage}}&limit={{limit}}">Página Anterior</a>
    {{/if}}

    {{#if hasNextPage}}
      <a href="/products?page={{nextPage}}&limit={{limit}}">Página Siguiente</a>
    {{/if}}
  </div>

  <script>
    function agregarAlCarrito(productId) {
      // Lógica para agregar el producto con ID productId al carrito
    }
  </script>
</body>
</html>
